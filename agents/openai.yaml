interface:
  display_name: "Ask DeepSeek"
  short_description: "通过已登录的 DeepSeek 网页获取可靠的第二意见"
  brand_color: "#4C6FFF"
  default_prompt: "使用 $ask-deepseek，通过 EGO Lite 中已登录的 DeepSeek 统一模型复核当前问题，并由 Codex 独立核验结果。"

policy:
  allow_implicit_invocation: true
